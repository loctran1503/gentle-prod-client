query GetPaginationUsers($skip:Float!){
  getPaginationUsers(skip:$skip){
    code
    success
    message
    totalCount
    userHideCount
    hasMore
    cursor
    users{
      userName
      userAvatar
    }
  }
}