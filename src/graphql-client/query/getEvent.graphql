query GetEvent($eventId:Float!){
    getEvent(eventId:$eventId){
        code
        success
        message
        myEvent{
            id
            title
            thumbnailForDesktop
            thumbnailForMobile
            content

            createdAt
        }
    }
}