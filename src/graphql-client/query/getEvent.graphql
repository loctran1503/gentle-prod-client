query GetEvent($title:String!){
    getEvent(title:$title){
        code
        success
        message
        myEvent{
            title
            thumbnailForDesktop
            thumbnailForMobile
            content

            createdAt
        }
    }
}